### Practice Questions

1. Create a string using double quotes and print its content using a loop.

    Solution: [Strings](./strings.c)

1. Which of the following is used to appropriately read a multi-word string.
    1. `gets()`
    1. `puts()`
    1. `printf()`
    1. `scanf()`

    Solution: `gets()` is used to read a multi-word string

1. Write a program to take string as an input from the user using `%c` and `%s` confirm that the strings are equal.

    Solution: [Strings - Input and Verification](./input_strings.c)

1. Write your own version of `strlen` function from `<string.h>`

    Solution: [Length of the String](./string_length.c)

